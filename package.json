{
  "name": "sepadan",
  "version": "1.0.0",
  "description": "Sepadan — parametric stablecoin depeg insurance built on GenLayer Intelligent Contracts",
  "private": true,
  "scripts": {
    "network": "genlayer network",
    "lint": "genvm-lint check contracts/sepadan.py",
    "deploy": "genlayer deploy",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "gltest --network studionet"
  },
  "devDependencies": {
    "genlayer-js": "^1.1.7",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2"
  }
}
